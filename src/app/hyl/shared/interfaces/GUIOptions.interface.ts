export interface GUIOptions {
  reactiveCorner: boolean,
  darkMode: boolean,
  lastUpdate: number
}

export const GUIOptionsDefault: GUIOptions = {
  reactiveCorner: true,
  darkMode: false,
  lastUpdate: 0
}
